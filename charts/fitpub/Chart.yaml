apiVersion: v2
name: fitpub
description: Helm chart for FitPub - federated fitness tracking platform
type: application
version: 0.3.7
appVersion: "1.1.1"
kubeVersion: ">= 1.26.0"
home: https://codeberg.org/fitpub/fitpub
sources:
  - https://codeberg.org/fitpub/fitpub
icon: https://raw.githubusercontent.com/oliinykdm/fitpub-helm/main/icon.png
keywords:
  - fitness
  - activitypub
  - spring-boot
  - postgresql
maintainers:
  - name: Dmytro Oliinyk
    url: https://github.com/oliinykdm
annotations:
  artifacthub.io/category: skip-prediction
  artifacthub.io/license: Apache-2.0
  artifacthub.io/prerelease: "false"
  artifacthub.io/containsSecurityUpdates: "false"
  artifacthub.io/images: |
    - name: fitpub
      image: codeberg.org/fitpub/fitpub:1.1.1
    - name: volume-permissions
      image: busybox:1.37
      whitelisted: true
  artifacthub.io/signKey: |
    fingerprint: E4C86577339895552E1CAB5E49BD9E5EA6C243B7
    url: https://oliinykdm.github.io/fitpub-helm/pgp-public-key.asc
  artifacthub.io/changes: |
    - kind: fixed
      description: Omit mail settings from the default ConfigMap until FITPUB_MAIL_HOST is set; avoids localhost:587 with forced SMTP auth.
    - kind: added
      description: Optional config keys for Hikari pool, ActivityPub inbox tuning, FITPUB_MAIL_PROTOCOL, OSM tiles and weather toggles.
    - kind: fixed
      description: Block multi-replica and RollingUpdate deployments with ReadWriteOnce upload volumes (including existingClaim).
    - kind: added
      description: CI runtime test for networkpolicy-smoke-values.yaml with restricted egress.
    - kind: fixed
      description: Documentation fixes (kubectl exec, README secret placeholders, ingress forwarded headers, application logs).
