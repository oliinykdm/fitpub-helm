apiVersion: v2
name: fitpub
description: Helm chart for FitPub - federated fitness tracking platform
type: application
version: 0.3.8
appVersion: "1.1.1"
kubeVersion: ">= 1.26.0"
home: https://codeberg.org/fitpub/fitpub
sources:
  - https://codeberg.org/fitpub/fitpub
icon: https://raw.githubusercontent.com/oliinykdm/fitpub-helm/main/icon.png
keywords:
  - fitness
  - activitypub
  - spring-boot
  - postgresql
maintainers:
  - name: Dmytro Oliinyk
    url: https://github.com/oliinykdm
annotations:
  artifacthub.io/category: skip-prediction
  artifacthub.io/license: Apache-2.0
  artifacthub.io/prerelease: "false"
  artifacthub.io/containsSecurityUpdates: "false"
  artifacthub.io/images: |
    - name: fitpub
      image: codeberg.org/fitpub/fitpub:1.1.1
    - name: volume-permissions
      image: busybox:1.37
      whitelisted: true
  artifacthub.io/signKey: |
    fingerprint: E4C86577339895552E1CAB5E49BD9E5EA6C243B7
    url: https://oliinykdm.github.io/fitpub-helm/pgp-public-key.asc
  artifacthub.io/changes: |
    - kind: fixed
      description: Render boolean/number config and secret values (false, 0) instead of silently dropping them; empty strings and null are still omitted.
    - kind: fixed
      description: Correct the README restricted-egress NetworkPolicy example (ClusterIP DNS now allows TCP 53; SMTP 587 is a separate rule).
    - kind: fixed
      description: Production example and README now provision FITPUB_MAIL_USERNAME/FITPUB_MAIL_PASSWORD when FITPUB_MAIL_SMTP_AUTH is true.
    - kind: added
      description: Document that the volume-permissions init container is incompatible with the restricted Pod Security Standard and how to disable it.
